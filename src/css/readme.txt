Just keeping the version of normalize used in style.css files, so it's
easy to compare with a new version. Either do a diff or replace with
a new version, and do a "git diff" to see exactly what has changed.